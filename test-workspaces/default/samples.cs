using System;
using System.IO;
class Program
{
    static void Main(string abc, int blobby)
    {
        // path of the file that we want to create
        string pathName = @"C:\Program\myFile.txt";
        const string SAMPLE = "blob";
        int num = 2;

        () => {};

        var x = {1, 2, 3};
        var y = true;

        // Create() creates a file at pathName 
        FileStream fs = File.Create(pathName);

        this.Close();

        if (x in stuff) {

        }

        // check if myFile.txt file is created at the specified path 
        if (!File.Exists(pathName))
        {
            Console.WriteLine("File is created.");
        }
        else
        {
            Console.WriteLine("File is not created.");
        }

        float yyy = 0.0;

        foreach(var i = 0; i < 10; i++) {

        }

        if (2 < 6 && stuff || 0) {
            Console.WriteLine("smurf");
        }

        return data;

        try {

        }
        catch () {

        }
        finally {

        }
    }
}